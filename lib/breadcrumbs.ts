import {styled} from '@/styled-system/jsx';
import {ark} from '@ark-ui/react';

export const Breadcrumbs = styled(ark.div);
export const BreadcrumbItem = styled(ark.div);
export const BreadcrumbLink = styled(ark.a);
export const BreadcrumbSeparator = styled(ark.svg);
