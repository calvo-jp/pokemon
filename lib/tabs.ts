'use client';

import {styled} from '@/styled-system/jsx';
import {Tabs as parts} from '@ark-ui/react';

export const Tabs = styled(parts.Root);
export const TabsContent = styled(parts.Content);
export const TabsIndicator = styled(parts.Indicator);
export const TabsList = styled(parts.List);
export const TabsTrigger = styled(parts.Trigger);
